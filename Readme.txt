To start this application, the nodejs and node-sass versions should be compatible with each other.
I tried by changing the nodejs version to 14 according to the node-sass version present but it can be done vice-versa.
Then install the node modules using the command npm i.
To start the application run npm start command 