import * as configure from './../../configFile'
export const config = {
    auth: {
                clientId : configure.clientId,
                authority: 'https://login.microsoftonline.com/common/',
                redirectUri: 'http://localhost:3000/Home'
              }
            };